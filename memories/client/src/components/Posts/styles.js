import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    mainContainer: {
        display: 'flex',
        alignItems: 'center',
    },
    smMargin: {
        margin: 1,
    },
    actionDiv: {
        textAlign: 'center',
    }
})

export default theme